/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    
    // domains: ['api.techsolution.com'],
    unoptimized: true,
  },
  // distDir: 'build',
  output: "export",
};

module.exports = nextConfig;
